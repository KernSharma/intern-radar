name: watch

on:
  schedule:
    - cron: "7,37 * * * *"  # every 30 min, offset to dodge top-of-hour congestion
  workflow_dispatch:
    inputs:
      bootstrap:
        description: "Mark everything currently live as seen (no notifications)"
        type: boolean
        default: false

permissions:
  contents: write
  issues: write

concurrency:
  group: watch
  cancel-in-progress: false

jobs:
  watch:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run watcher
        env:
          PYTHONPATH: src
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          RADAR_BOOTSTRAP: ${{ inputs.bootstrap == true && 'true' || 'false' }}
        run: python -m intern_radar

      - name: Commit seen state
        run: |
          git config user.name "intern-radar[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/seen.json
          if ! git diff --cached --quiet; then
            git commit -m "chore: update seen state [skip ci]"
            git pull --rebase origin ${GITHUB_REF_NAME}
            git push
          fi
